# TODO: 1:1 Baileys -> Python Reimplementation

This document tracks a strict file-level port from Baileys to a Python package with ABC-driven extension points.

- Source root: Baileys/
- Python root: wassupweb/
- Total files tracked: 134
- Status key: pending, in_progress, ported, validated
- Scope note: call support/call events are intentionally excluded per user requirement.
- API note: canonical `user_id` resolver added to reduce PN/LID/JID ambiguity for AI workflows.

## File Port Matrix

| Status | Source | Python Target | Notes |
|---|---|---|---|
| ported | Baileys/src/__tests__/binary/connection-deadlock.test.ts | tests/binary/connection_deadlock_test.py | Added pytest parity coverage ensuring buffered events flush and regular upserts are not blocked when history sync is disabled |
| ported | Baileys/src/__tests__/e2e/fetch-wa-web-version.test-e2e.ts | tests/e2e/fetch_wa_web_version_test_e2e.py | Added integration-shape pytest coverage for `fetch_latest_wa_web_version` including fallback behavior under short timeout |
| ported | Baileys/src/__tests__/e2e/send-receive-message.test-e2e.ts | tests/e2e/send_receive_message_test_e2e.py | Added guarded real-env e2e harness test (skips unless `WASSUPWEB_RUN_REAL_E2E=1`) |
| ported | Baileys/src/__tests__/proto-tojson-long.test.ts | tests/proto_tojson_long_test.py | Added pytest parity test for WAProto long-field JSON serialization (validated against generated Python statics) |
| ported | Baileys/src/__tests__/Signal/Group/sender-key-state-regression.test.ts | tests/signal/group/sender_key_state_regression_test.py | Added pytest parity coverage for legacy senderMessageKeys initialization regression |
| ported | Baileys/src/Signal/Group/*.ts (API surface) | tests/signal/group/signal_group_api_aliases_test.py | Added parity tests for Baileys-style camelCase API compatibility across keyhelper and sender-key classes (`getIteration`, `getSenderMessageKey`, `isEmpty`, etc.), plus package-level camelCase helper exports |
| ported | Baileys/src/Signal/Group/group_cipher.ts + group-session-builder.ts (storage interface) | tests/signal/group/group_storage_compat_test.py | Added parity tests ensuring sender-key storage adapters work with both snake_case and Baileys camelCase store methods (`load_sender_key`/`loadSenderKey`, `store_sender_key`/`storeSenderKey`) for plug-and-play repositories |
| ported | Baileys/src/__tests__/Signal/lid-mapping.test.ts | tests/signal/lid_mapping_test.py | Added pytest parity coverage for hosted-device PN reverse mapping and missing-mapping path. Latest pass adds class-instance camelCase alias coverage (`storeLIDPNMappings`, `getLIDsForPNs`, `getPNForLID`, etc.) |
| ported | Baileys/src/Signal/libsignal.ts | tests/signal/libsignal_repository_test.py | Added parity tests for global/repository camelCase aliases (`jidToSignalProtocolAddress`, `migrateSession`, `lidMapping`) and hosted-device migration session lookup via signal-address keys |
| ported | Baileys/src/__tests__/Socket/identity-change-handling.test.ts | tests/socket/identity_change_handling_test.py | Added pytest parity coverage for self/companion/debounce/offline/error handling branches |
| ported | Baileys/src/__tests__/TestUtils/session.ts | tests/testutils/session.py | Added temporary auth-state session helper (`make_session`) + websocket mock patch utility |
| ported | Baileys/src/__tests__/Utils/generics.test.ts | tests/utils/test_generics.py | Expanded BufferJSON parity coverage (roundtrip, legacy buffers, non-buffer objects, null/empty handling) |
| ported | Baileys/src/__tests__/Utils/history.test.ts | tests/utils/test_history.py | Expanded LID/PN mapping parity coverage across sync types, hosted IDs, group exclusions, and receipt fallback edge cases |
| ported | Baileys/src/__tests__/Utils/messages-media.test.ts | tests/utils/test_messages_media.py | Expanded parity coverage for upload redirects/timeouts, media-retry proto encrypt/decode/decrypt flow, status-code mapping, extension derivation, and ranged/full encrypted download decryption |
| ported | Baileys/src/__tests__/Utils/noise-handler.test.ts | tests/utils/test_noise_handler.py | Expanded parity coverage for frame decoding concurrency/splits plus handshake certificate validation branches (missing payload, invalid signature, valid chain path) |
| ported | Baileys/src/__tests__/Utils/process-message.test.ts | tests/utils/test_process_message.py | Expanded cleanMessage parity coverage for hosted/LID normalization, group passthrough, reaction perspective, and edge-case safety |
| ported | Baileys/src/__tests__/Utils/process-sync-action.test.ts | tests/utils/test_process_sync_action.py | Added mute/archive/mark-read/delete/contact/pushname/label + unknown-action branches. Latest pass adds `unarchiveChatsSetting` account-settings mutation parity, star-state index fallback handling, `lidContactAction` upsert payload coverage, and `notificationActivitySettingAction` settings update fanout. |
| ported | Baileys/src/Socket/chats.ts | tests/utils/test_chats_app_patch.py | Added parity tests for appPatch/fetchProps/wrapper methods/tctoken subscription + `createCallLink` query/token flow. Latest pass adds upsert parity coverage for participant-first contact identity (`participant || remoteJid`), camelCase `placeholderResendCache` fallback usage, sync callable `get_message` compatibility, non-blocking available-presence unified-session behavior (failure isolation), and typed chat convenience wrapper coverage for Pydantic input models/aliases. |
| ported | Baileys/src/Socket/groups.ts | tests/utils/test_groups_socket.py | Added parity tests for metadata parsing, invite-v4/upsert, participant approvals, dirty refresh. Latest pass adds coverage that `groupAcceptInviteV4` is routed through a cached buffered runner (`createBufferedFunction`/`create_buffered_function`) and not re-wrapped per call; also adds typed convenience-interface coverage for new Pydantic wrapper methods (`create_group`, `update_group_*`) including alias-field handling (`ephemeralExpiration`). |
| ported | Baileys/src/Socket/communities.ts | tests/utils/test_communities_socket.py | Added parity tests for `parseGroupResult` using `groupMetadata` path, linked-group parent resolution via group metadata, and `communityAcceptInviteV4` routing through `upsertMessage` with invite-expiry `messages.update` event. Latest pass also validates buffered-runner caching for invite-v4 accept handlers, and adds typed convenience-interface coverage for new Pydantic wrapper methods (`create_community`, `update_community_*`, invite wrappers) including alias-field handling (`parentCommunityJid`, `inviteMessage`, `ephemeralExpiration`). |
| ported | Baileys/src/Socket/business.ts | tests/utils/test_business_socket.py | Added parity tests for business-profile delta node composition, parsed `getBusinessProfile` shape, and uploader resolution order (`wa_upload_to_server` attribute before config fallback). Latest pass adds catalog/collections query-composition coverage (default self JID resolution, cursor/limit/width/height/smax attrs), product-delete deleted-count parsing parity, dict-input validation path for `update_business_profile`, and typed convenience-interface coverage for new Pydantic business wrappers (`fetch_business_profile`, `set_business_profile`, `fetch_catalog`, `update_product`, `delete_products`, etc.) including alias-field handling (`orderId`, `tokenBase64`, `productId`, `productIds`, `photoId`). |
| ported | Baileys/src/Socket/newsletter.ts | tests/utils/test_newsletter_mex.py | Existing tests covered MEX error/shape + picture-base64 behavior. Latest pass adds typed convenience-interface coverage for new Pydantic newsletter wrappers (`create_newsletter`, `update_newsletter`, `follow/unfollow`, `react/fetch`, admin/owner/demote/delete helpers) including alias-field handling (`serverId`, `newOwnerJid`, `userJid`). |
| ported | Baileys/src/Socket/socket.ts | tests/utils/test_core_socket.py | Added parity tests for awaitNextMessage, prekey checks, requestPairingCode/sendWAMBuffer, CB/TAG callbacks, offline/success lifecycle handlers, `waitForMessage` timeout->`None` behavior, `waitForSocketOpen` closed-state rejection, own PN/LID migration scheduling on login success, connect-time `mobile`/`tcp` rejections, deprecated `printQRInTerminal` warning, all-sync-disabled danger warning path, routing-info `ED` query injection for `wss` transport URLs, and `_is_transport_open` callable-error guard (must evaluate as closed). |
| ported | Baileys namespace import compatibility (capitalized paths) | tests/utils/test_compat_module_shims.py | Expanded compatibility tests for root-level Baileys-style import namespaces and nested paths (`Defaults`, `Socket.Client`, `Signal.Group`, `WAUSync.Protocols`, etc.). Latest pass adds broader `Socket.*` and `Types.*` root alias coverage (`Socket.MessagesSend`, `Socket.Communities`, `Types.Community`, `Types.Newsletter`, etc.). |
| ported | Baileys/src/Utils/event-buffer.ts | tests/utils/test_event_bus.py | Added buffering/flush and createBufferedFunction behavior tests |
| ported | Baileys/src/__tests__/Utils/reporting-utils.test.ts | tests/utils/test_reporting_utils.py | Added pytest parity coverage for include/exclude rules + reporting token generation/determinism/uniqueness |
| ported | Baileys/src/__tests__/Utils/signal.test.ts | tests/utils/test_signal.py | Added pytest parity coverage for parseAndInjectE2ESessions user-node processing |
| ported | Baileys/src/__tests__/Utils/sync-action-utils.test.ts | tests/utils/test_sync_action_utils.py | Expanded pytest parity coverage for PN/LID mapping rules (including device-suffixed LIDs), missing/invalid/group edge cases, id-vs-pnJid precedence, and `emitSyncActionResults` event fanout |
| ported | Baileys/src/WAUSync/USyncQuery.ts | tests/utils/test_wausync_query.py | Added parity tests for USync result parsing across `list` + `side_list`, Baileys-style camelCase query/user helper aliases, and protocol-level `getQueryElement`/`getUserElement` alias behavior |
| ported | Baileys/src/Socket/Client/index.ts | tests/utils/test_client_socket.py | Added parity/API tests for client identity alias surface (`resolveUser*`, `resolveChatJid`, `linkIdentity`, `resolveMessageIdentity`) plus existing wait/recv lifecycle behaviors |
| ported | Baileys/src/Defaults/baileys-version.json | wassupweb/defaults/baileys_version.json | Direct logic port |
| ported | Baileys/src/Defaults/index.ts | wassupweb/defaults/index.py | Direct logic port; added `PROCESSABLE_HISTORY_TYPES` parity constant used by socket sync-disable safety checks and restored default config keys for `mobile`/`agent`/`fetchAgent`/`printQRInTerminal` |
| ported | Baileys/src/index.ts | wassupweb/index.py | Direct logic port; root package now also registers Baileys-style capitalized namespace aliases (`Socket`, `Signal`, `Types`, `WABinary`, `WAM`, `WAUSync`, `WAProto`) in `wassupweb/__init__.py` for compatibility imports, including expanded `Socket.*` and `Types.*` submodule aliases |
| ported | Baileys/src/Signal/Group/ciphertext-message.ts | wassupweb/signal/group/ciphertext_message.py | Direct logic port |
| ported | Baileys/src/Signal/Group/group_cipher.ts | wassupweb/signal/group/group_cipher.py | Direct logic port + storage-method compatibility bridge supporting snake_case and camelCase store APIs (`load_sender_key`/`loadSenderKey`, `store_sender_key`/`storeSenderKey`) |
| ported | Baileys/src/Signal/Group/group-session-builder.ts | wassupweb/signal/group/group_session_builder.py | Direct logic port + storage-method compatibility bridge supporting snake_case and camelCase store APIs (`load_sender_key`/`loadSenderKey`, `store_sender_key`/`storeSenderKey`) |
| ported | Baileys/src/Signal/Group/index.ts | wassupweb/signal/group/index.py | Compatibility re-export shim + group package now exports Baileys-style camelCase keyhelper names |
| ported | Baileys/src/Signal/Group/keyhelper.ts | wassupweb/signal/group/keyhelper.py | Direct logic port + Baileys-style camelCase function aliases (`generateSenderKey`, `generateSenderKeyId`, `generateSenderSigningKey`) |
| ported | Baileys/src/Signal/Group/sender-chain-key.ts | wassupweb/signal/group/sender_chain_key.py | Direct logic port + camelCase method aliases (`getIteration`, `getSenderMessageKey`, `getNext`, `getSeed`) |
| ported | Baileys/src/Signal/Group/sender-key-distribution-message.ts | wassupweb/signal/group/sender_key_distribution_message.py | Direct logic port + camelCase method aliases (`getType`, `getIteration`, `getChainKey`, `getSignatureKey`, `getId`) |
| ported | Baileys/src/Signal/Group/sender-key-message.ts | wassupweb/signal/group/sender_key_message.py | Direct logic port + camelCase method aliases (`getKeyId`, `getIteration`, `getCipherText`, `verifySignature`, `getType`) |
| ported | Baileys/src/Signal/Group/sender-key-name.ts | wassupweb/signal/group/sender_key_name.py | Direct logic port + camelCase method aliases (`getGroupId`, `getSender`, `hashCode`) |
| ported | Baileys/src/Signal/Group/sender-key-record.ts | wassupweb/signal/group/sender_key_record.py | Direct logic port + camelCase method aliases (`isEmpty`, `getSenderKeyState`, `addSenderKeyState`, `setSenderKeyState`) |
| ported | Baileys/src/Signal/Group/sender-key-state.ts | wassupweb/signal/group/sender_key_state.py | Direct logic port + camelCase method aliases (`getKeyId`, `getSenderChainKey`, `setSenderChainKey`, `getStructure`, etc.) |
| ported | Baileys/src/Signal/Group/sender-message-key.ts | wassupweb/signal/group/sender_message_key.py | Direct logic port + camelCase method aliases (`getIteration`, `getIv`, `getCipherKey`, `getSeed`) |
| ported | Baileys/src/Signal/libsignal.ts | wassupweb/signal/libsignal.py | Direct logic port + camelCase aliases on repository/global helpers; migration parity hardening for PN/LID validation (`isPnUser`/`isLidUser`) and hosted-device session lookup using signal-address fetch |
| ported | Baileys/src/Signal/lid-mapping.ts | wassupweb/signal/lid_mapping.py | Direct logic port + class-level camelCase method aliases on `LIDMappingStore` (in addition to module-level aliases) for 1:1 Baileys-style invocation |
| ported | Baileys/src/Socket/business.ts | wassupweb/socket/business.py | Added delta-style updateBusinessProfile node builder + camelCase compatibility aliases; aligned `get_business_profile` behavior with chats-layer parsed profile shape and uploader selection parity. Latest pass lets `update_business_profile` accept both `UpdateBusinessProfileProps` models and plain dict input via Pydantic validation, and adds Pydantic-backed convenience wrappers (`fetch_business_profile`, `set_business_profile`, `update_business_cover_photo`, `fetch_catalog`, `fetch_collections`, `fetch_order_details`, `update_product`, `create_product`, `delete_products`) with camelCase aliases for easier API usage. |
| ported | Baileys/src/Socket/chats.ts | wassupweb/socket/chats.py | Added appPatch/resync/fetchProps/init-queries/chatModify wrappers + upsert sync-state handlers + tctoken presence/profile paths; added `createCallLink` call query/token method; message-node handling now delegates to recv-layer decrypt/retry/receipt pipeline for parity. Latest pass aligns `upsertMessage` participant selection (`participant || remoteJid`) for contact updates, adds camel/snake placeholder resend cache compatibility, hardens creds/getMessage serialization for non-Pydantic auth objects, updates available presence flow to fire-and-forget `sendUnifiedSession` like Baileys, and adds full typed chat convenience wrappers (modify/contact/labels/quick-replies/archive/mute/read/usync/profile/block/presence/call-link) with camelCase aliases. |
| ported | Baileys/src/Socket/Client/index.ts | wassupweb/socket/client.py | Merged into pythonic client surface; added resolver-aware helpers (`resolve_user_ref`, `resolve_message_identity`) and `resolve_chat_jid(..., prefer=PN/LID)` for stable AI-facing identity handling. Latest pass adds camelCase API aliases for parity/ease-of-use (`resolveUser`, `resolveUserRef`, `resolveChatJid`, `linkIdentity`, `resolveMessageIdentity`). |
| ported | Baileys/src/Socket/Client/types.ts | wassupweb/socket/client.py | Merged into pythonic client surface |
| ported | Baileys/src/Socket/Client/websocket.ts | wassupweb/socket/transport.py | Transport lifecycle parity: explicit open-state property + modern websockets compatibility |
| ported | Baileys/src/Socket/communities.ts | wassupweb/socket/communities.py | Method surface + metadata/invite/settings + dirty-bit refresh handler ported. Latest pass aligns `parseGroupResult` and linked-group parent discovery to use inherited `groupMetadata` (Baileys behavior), routes `communityAcceptInviteV4` stub insertion through `upsert_message` (fallback to event emit only if unavailable), wraps invite-v4 accept execution in a cached buffered runner for Baileys-style ordered buffering, and adds Pydantic-backed convenience wrappers (`create_community`, `create_community_group`, `leave_community`, `update_community_*`, invite helpers) with camelCase aliases for easier API usage. |
| ported | Baileys/src/Socket/groups.ts | wassupweb/socket/groups.py | Expanded to full groupQuery/metadata/invite/member/approval/dirty-sync surface + extractGroupMetadata parity parser. Latest pass aligns `groupAcceptInviteV4` with Baileys buffered-handler semantics by caching and reusing a `createBufferedFunction`/`create_buffered_function` runner, and adds Pydantic-backed convenience wrappers (`create_group`, `update_group_participants`, `update_group_requests`, `update_group_subject`, etc.) with camelCase aliases for easier API usage. |
| ported | Baileys/src/Socket/index.ts | wassupweb/socket/index.py | Factory parity: default buffered event emitter wiring + full socket stack creation |
| ported | Baileys/src/Socket/messages-recv.ts | wassupweb/socket/messages_recv.py | Expanded parity: ack semantics (`sendMessageAck`), bad-ack error propagation (`handleBadAck`), Baileys-like receipt key derivation (`fromMe`/`remoteJid`) and status fanout (`messages.update`/`message-receipt.update`), retry resend helpers (`willSendMessageAgain`, `updateSendMessageAgainCount`, `sendMessagesAgain`, `sendRetryRequest`) now integrated with `MessageRetryManager` cache/accounting + scheduled phone resend path, newsletter+mex+mediaretry+privacy-token notification handling, `server_sync`/`account_sync` notification handling (resync + disappearing mode + blocklist), PDO helpers (`sendPeerDataOperationMessage`, `fetchMessageHistory`, `requestPlaceholderResend`), additional notification parity for `w:gp2` group notifications, `picture` updates, `encrypt` prekey/identity processing, and `link_code_companion_reg` companion-finish flow; message handling now uses decrypt/retry/receipt flow instead of raw placeholder upsert. Latest parity pass added internal placeholder-resend cache fallback when `placeholderResendCache` is not configured, external `msgRetryCounterCache` read/write/delete parity integration (with dict fallback), Baileys-like node dispatch wrappers for per-node event buffer + offline-node queued processing, PDO send path alignment to peer protocol-message relay semantics, and account-model-safe device identity encoding in retry-request key bundle path. |
| ported | Baileys/src/Socket/messages-send.ts | wassupweb/socket/messages_send.py | Expanded parity beyond receipts: implemented `refreshMediaConn` + `wa_upload_to_server` wiring, `getUSyncDevices` (including external `userDevicesCache` get/mget + set/mset support), `assertSessions`, `createParticipantNodes`, and encrypted `relayMessage` participant fanout (direct/group/newsletter/retry paths) with sender-key handling, reporting-token/tctoken attachment, plus `getMessageType`/`getMediaType`/`getPrivacyTokens`/`updateMemberLabel`/`sendPeerDataOperationMessage`/`updateMediaMessage` and richer `sendMessage` generation/edit-meta routing (including link-preview/profile/upload/call-link generation hooks). Added typed convenience send interfaces (`send`, `send_text`/`sendText`) backed by Pydantic request models. Latest parity pass added retry `device_fanout=false` attr for non-group participant resends, status-path cached-group-metadata suppression, camelCase LID-mapping method fallbacks (`storeLIDPNMappings`/`getLIDsForPNs`, repo `validateSession`/`jidToSignalProtocolAddress`), race-safe media-update wait task in `update_media_message`, `mediaCache` propagation into message generation opts, direct-chat relay honoring `use_user_devices_cache` option during device enumeration, account-model-safe device identity encoding in relay path, and structured identity targets (`UserRef`/dict with `userId`/`pnJid`/`lidJid`) in typed send inputs to reduce PN/LID/JID ambiguity. Added pytest coverage for these in `tests/utils/test_messages_send_socket.py`. |
| ported | Baileys/src/Socket/mex.ts | wassupweb/socket/mex.py | Query + execute helpers ported with structured GraphQL error metadata parity (+ pytest coverage in tests/utils/test_newsletter_mex.py) |
| ported | Baileys/src/Socket/newsletter.ts | wassupweb/socket/newsletter.py | Method surface parity + picture update payload corrected to base64 as in Baileys. Latest pass adds Pydantic-backed convenience wrappers (`create_newsletter`, `update_newsletter`, `fetch_newsletter_metadata`, `react_to_newsletter_message`, `change_newsletter_owner`, etc.) with camelCase aliases for easier API usage (+ pytest coverage in tests/utils/test_newsletter_mex.py). |
| ported | Baileys/src/Socket/socket.ts | wassupweb/socket/socket.py | Added connect handshake flow, awaitNextMessage/waitForSocketOpen/keepalive, requestPairingCode/sendWAMBuffer, CB/TAG callback emission, pair-device/pair-success/success/stream-error/failure/ib-offline handlers, improved prekey upload/verification, websocket-error mapping helper, sendRawMessage closed/timeout guards, waitForMessage timeout parity, success-path own PN/LID mapping + session migration scheduling, and connect-time parity checks/warnings for `mobile`, `tcp`, `printQRInTerminal`, all-history-sync-disabled danger path, plus `routingInfo` -> `ED` websocket query propagation for `wss` URLs. Latest hardening fixes callable `is_open()` exception handling to treat transport as closed (instead of truthy callable fallback) and replaces several silent exception `pass` branches with explicit debug/fallback paths. |
| ported | Baileys/src/Types/Auth.ts | wassupweb/types/Auth.py | Added missing auth parity models and identity-shape compatibility: `ProtocolAddress`, `SignalCreds`, `SignalAuthState`, `LIDMapping`, `LTHashState` (+ `indexValueMap` parsing), and dual-shape `SignalIdentity` support (`identifier.{name,deviceId}` and flat fields). Added dedicated pytest coverage in `tests/types/test_auth_types.py`. |
| ported | Baileys/src/Types/Bussines.ts | wassupweb/types/business.py | Pydantic models for business-hours/profile updates + quick reply action; added Pydantic request models for typed business operation wrappers (`BusinessProfileInput`, `BusinessCatalogInput`, `BusinessOrderDetailsInput`, `BusinessProductUpdateInput`, `BusinessProductDeleteInput`, etc.) |
| ported | Baileys/src/Types/Call.ts | wassupweb/types/Call.py | Completed call model parity (`WACallEvent`, `WACallUpdateType`) and wired exports through `types/__init__.py` plus root compat alias coverage (`wassupweb.Types.Call`) with pytest verification in `tests/utils/test_compat_module_shims.py`. |
| ported | Baileys/src/Types/Chat.ts | wassupweb/types/Chat.py | Core chat/privacy/patch models ported |
| ported | Baileys/src/Types/Contact.ts | wassupweb/types/Contact.py | Contact model ported |
| ported | Baileys/src/Types/Events.ts | wassupweb/types/Events.py | Event payload + buffered structures ported; added typed `messages.media-update` payload model to match newly handled media-retry notification stream |
| ported | Baileys/src/Types/globals.d.ts | wassupweb/types/globals.pyi | Added `RequestInit` TypedDict with `dispatcher` and `duplex` parity fields |
| ported | Baileys/src/Types/GroupMetadata.ts | wassupweb/types/GroupMetadata.py | Group metadata/participant models ported; added Pydantic input models for typed group operations (`GroupCreateInput`, `GroupParticipantsUpdateInput`, `GroupRequestParticipantsUpdateInput`, `GroupSubjectUpdateInput`, `GroupDescriptionUpdateInput`, `GroupSettingUpdateInput`, `GroupMemberAddModeInput`, `GroupJoinApprovalModeInput`, `GroupToggleEphemeralInput`) |
| ported | Baileys/src/Types/index.ts | wassupweb/types/index.py | Re-export surface validated with expanded compatibility coverage for Baileys-style module paths, including full root `wassupweb.Types.*` alias matrix (`Business`, `Call`, `Chat`, `Contact`, `Events`, `Label`, `Signal`, `State`, `USync`) in `wassupweb/__init__.py` + pytest checks. |
| ported | Baileys/src/Types/Label.ts | wassupweb/types/Label.py | Label models ported |
| ported | Baileys/src/Types/LabelAssociation.ts | wassupweb/types/LabelAssociation.py | Label association models ported |
| ported | Baileys/src/Types/Message.ts | wassupweb/types/Message.py | Core message/media/stub models ported; updated `WAMessageStubType` values to match WA proto enum numbers, added missing group/system constants, and added explicit `CIPHERTEXT` stub constant for decrypt-failure parity paths |
| ported | Baileys/src/Types/Newsletter.ts | wassupweb/types/Newsletter.py | Newsletter metadata/update models ported; added Pydantic request models for typed newsletter operations (`NewsletterCreateInput`, `NewsletterUpdateInput`, `NewsletterReactInput`, `NewsletterFetchMessagesInput`, `NewsletterChangeOwnerInput`, `NewsletterDemoteInput`, etc.) |
| ported | Baileys/src/Types/Product.ts | wassupweb/types/Product.py | Product/catalog/order models ported |
| ported | Baileys/src/Types/Signal.ts | wassupweb/types/Signal.py | Signal repository option models/protocols ported |
| ported | Baileys/src/Types/Socket.ts | wassupweb/types/Socket.py | Added missing cache-related config fields (`mediaCache`, `msgRetryCounterCache`, `userDevicesCache`, `callOfferCache`) and restored additional config parity fields (`mobile`, `agent`, `fetchAgent`, `printQRInTerminal`) |
| ported | Baileys/src/Types/State.ts | wassupweb/types/State.py | Connection/sync state models ported |
| ported | Baileys/src/Types/USync.ts | wassupweb/types/USync.py | Direct logic port |
| ported | Baileys/src/Utils/auth-utils.ts | wassupweb/utils/auth_utils.py | Direct logic port. Latest pass improves parity for transaction/caching behavior: pre-key deletion validation on non-transaction writes, keyed serialized per-type writes, transaction-context cache reads with db-query tracking, retrying commits, and logger trace/warn/error hooks. Added pytest coverage in `tests/utils/test_auth_utils.py`. |
| ported | Baileys/src/Utils/browser-utils.ts | wassupweb/utils/browser_utils.py | Direct logic port |
| ported | Baileys/src/Utils/business.ts | wassupweb/utils/business.py | Direct logic port. Latest pass aligns upload concurrency with Baileys (`Promise.all`-style parallel image uploads), keeps direct-path URL normalization parity, and adds dedicated parser/product/upload pytest coverage in `tests/utils/test_business_utils.py`. |
| ported | Baileys/src/Utils/chat-utils.ts | wassupweb/utils/chat_utils.py | Direct logic port. Latest pass improves app-state extraction parity for snapshots: `extract_syncd_patches` now resolves external snapshot blob references via `download_external_blob` and decodes downloaded snapshot payloads; also keeps patch-version fallback behavior. Added dedicated coverage in `tests/utils/test_chat_utils.py`. |
| ported | Baileys/src/Utils/crypto.ts | wassupweb/utils/crypto.py | Core curve/hkdf/aes/signature helpers ported |
| ported | Baileys/src/Utils/decode-wa-message.ts | wassupweb/utils/decode_wa_message.py | Decoding/decryption/context extraction helpers ported; decrypt payload parsing now prefers WAProto `Message` decode (with JSON fallback only when needed) and decrypt-failure/no-message paths now emit `CIPHERTEXT` stub type parity |
| ported | Baileys/src/Utils/event-buffer.ts | wassupweb/utils/event_buffer.py | Direct logic port |
| ported | Baileys/src/Utils/generics.ts | wassupweb/utils/generics.py | Direct logic port |
| ported | Baileys/src/Utils/history.ts | wassupweb/utils/history.py | Direct logic port |
| ported | Baileys/src/Utils/identity-change-handler.ts | wassupweb/utils/identity_change_handler.py | Identity-change handling helper ported |
| ported | custom identity abstraction (AI helper) | wassupweb/utils/identity.py | Added `resolve_message_identity` + Pydantic `MessageIdentityView`/`SendMessageInput`/`SendTextInput` support to reduce PN/LID/JID ambiguity; latest pass allows structured `to` identity payloads (`UserRef`/dict) in typed send inputs so agents can send by canonical user identity without manual JID string assembly. |
| ported | custom community abstraction (typed API helper) | wassupweb/types/community.py | Added dedicated Pydantic request models for community operations (`CommunityCreateInput`, `CommunityCreateGroupInput`, `CommunityAcceptInviteV4Input`, `CommunityToggleEphemeralInput`, etc.) and exported via `types/__init__.py` with compatibility alias `types.Community` |
| ported | custom newsletter abstraction (typed API helper) | wassupweb/types/newsletter.py | Added dedicated Pydantic request models for newsletter operations (`NewsletterCreateInput`, `NewsletterUpdateInput`, `NewsletterJidInput`, `NewsletterMetadataInput`, `NewsletterReactInput`, `NewsletterChangeOwnerInput`, `NewsletterDemoteInput`) and wired wrappers in `socket/newsletter.py` |
| ported | Baileys/src/Utils/index.ts | wassupweb/utils/index.py | Direct logic port. Latest pass adds package-level lazy export forwarding in `wassupweb/utils/__init__.py` so `wassupweb.utils` exposes Baileys utility surface without circular-import startup regressions, plus parity coverage in `tests/utils/test_utils_index.py` (snake_case + camelCase symbols). |
| ported | Baileys/src/Utils/link-preview.ts | wassupweb/utils/link_preview.py | Direct logic port |
| ported | Baileys/src/Utils/logger.ts | wassupweb/utils/logger.py | Direct logic port |
| ported | Baileys/src/Utils/lt-hash.ts | wassupweb/utils/lt_hash.py | Direct logic port |
| ported | Baileys/src/Utils/make-mutex.ts | wassupweb/utils/make_mutex.py | Async mutex helper ported |
| ported | Baileys/src/Utils/message-retry-manager.ts | wassupweb/utils/message_retry_manager.py | Retry manager/cache/session-recreate logic ported |
| ported | Baileys/src/Utils/messages.ts | wassupweb/utils/messages.py | Expanded media-content parity: `generate_wa_message_content` now prepares/uploads raw image/video/audio/document/sticker payloads via encrypted media stream + upload callback, populates key hashes/length/timestamps, default mimetypes, caption/filename/media flags, and thumbnail/dimension hints where available; pre-uploaded media payloads remain passthrough. Added exported `prepareWAMessageMedia` wrapper parity, event-call `joinLink` generation via `getCallLink` + call URL prefixes, plus `downloadMediaMessage` wrapper parity (buffer/stream, thumbnail direct-path mode, reupload-on-404/410 flow). Latest passes align fallback behavior to call `prepareWAMessageMedia` (instead of raw passthrough), apply context-info after edit wrapping, inject reporting-token `messageSecret` when eligible, mirror quoted-message handling by preferring `quoted.participant` and stripping nested quoted `contextInfo`, and propagate `jid` into content generation (newsletter/media parity) with logger child-msgId behavior in `generate_wa_message`. Added tests in `tests/utils/test_messages_utils.py`. |
| ported | Baileys/src/Utils/messages-media.ts | wassupweb/utils/messages_media.py | Improved parity: media-retry now uses WAProto ServerErrorReceipt/MediaRetryNotification when available, corrected ResultType->status mapping, extension derivation behavior, and implemented chunk/range-aware CBC download decryption flow |
| ported | Baileys/src/Utils/noise-handler.ts | wassupweb/utils/noise_handler.py | Improved parity: handshake now requires payload and validates CertChain signatures/issuer against WA cert details (with WAProto-aware class resolution + compatibility fallback) |
| ported | Baileys/src/Utils/pre-key-manager.ts | wassupweb/utils/pre_key_manager.py | Direct logic port |
| ported | Baileys/src/Utils/process-message.ts | wassupweb/utils/process_message.py | Direct logic port; replaced encrypted event/poll response placeholder with actual HMAC+AES-GCM derivation/decryption + WAProto decode helpers (`decrypt_event_response`, `decrypt_poll_vote`) and added roundtrip parity tests |
| ported | Baileys/src/Utils/reporting-utils.ts | wassupweb/utils/reporting_utils.py | Reporting token field compiler/extractor/hmac logic ported + pytest coverage added |
| ported | Baileys/src/Utils/signal.ts | wassupweb/utils/signal.py | Direct logic port |
| ported | Baileys/src/Utils/sync-action-utils.ts | wassupweb/utils/sync_action_utils.py | Direct logic port |
| ported | Baileys/src/Utils/tc-token-utils.ts | wassupweb/utils/tc_token_utils.py | TC token builder helper ported |
| ported | Baileys/src/Utils/use-multi-file-auth-state.ts | wassupweb/utils/use_multi_file_auth_state.py | Direct logic port |
| ported | Baileys/src/Utils/validate-connection.ts | wassupweb/utils/validate_connection.py | Login/registration/pair-success helpers ported and wired into core socket flow. Latest pass fixes pairing HMAC secret decoding parity (`advSecretKey` base64) and hardens signature-field decoding (base64/hex/raw) for device/account verification paths. Added pytest coverage in `tests/utils/test_validate_connection.py`. |
| ported | Baileys/src/WABinary/constants.ts | wassupweb/wabinary/constants.py | Direct logic port |
| ported | Baileys/src/WABinary/decode.ts | wassupweb/wabinary/decode.py | Direct logic port |
| ported | Baileys/src/WABinary/encode.ts | wassupweb/wabinary/encode.py | Direct logic port |
| ported | Baileys/src/WABinary/generic-utils.ts | wassupweb/wabinary/generic_utils.py | Direct logic port |
| ported | Baileys/src/WABinary/index.ts | wassupweb/wabinary/index.py | Direct logic port |
| ported | Baileys/src/WABinary/jid-utils.ts | wassupweb/wabinary/jid_utils.py | Direct logic port |
| ported | Baileys/src/WABinary/types.ts | wassupweb/wabinary/types.py | Direct logic port |
| ported | Baileys/src/WAM/BinaryInfo.ts | wassupweb/wam/BinaryInfo.py | Direct logic port |
| ported | Baileys/src/WAM/constants.ts | wassupweb/wam/constants.py | Direct logic port |
| ported | Baileys/src/WAM/encode.ts | wassupweb/wam/encode.py | Direct logic port |
| ported | Baileys/src/WAM/index.ts | wassupweb/wam/index.py | Direct logic port |
| ported | Baileys/src/WAUSync/index.ts | wassupweb/wausync/index.py | Direct logic port |
| ported | Baileys/src/WAUSync/Protocols/index.ts | wassupweb/wausync/protocols/index.py | Direct logic port |
| ported | Baileys/src/WAUSync/Protocols/UsyncBotProfileProtocol.ts | wassupweb/wausync/protocols/bot_profile.py | Direct logic port + Baileys-style `getQueryElement`/`getUserElement` aliases |
| ported | Baileys/src/WAUSync/Protocols/USyncContactProtocol.ts | wassupweb/wausync/protocols/contact.py | Direct logic port + Baileys-style `getQueryElement`/`getUserElement` aliases |
| ported | Baileys/src/WAUSync/Protocols/USyncDeviceProtocol.ts | wassupweb/wausync/protocols/device.py | Direct logic port + Baileys-style `getQueryElement`/`getUserElement` aliases |
| ported | Baileys/src/WAUSync/Protocols/USyncDisappearingModeProtocol.ts | wassupweb/wausync/protocols/disappearing_mode.py | Direct logic port + Baileys-style `getQueryElement`/`getUserElement` aliases |
| ported | Baileys/src/WAUSync/Protocols/UsyncLIDProtocol.ts | wassupweb/wausync/protocols/lid.py | Direct logic port + Baileys-style `getQueryElement`/`getUserElement` aliases |
| ported | Baileys/src/WAUSync/Protocols/USyncStatusProtocol.ts | wassupweb/wausync/protocols/status.py | Direct logic port + Baileys-style `getQueryElement`/`getUserElement` aliases |
| ported | Baileys/src/WAUSync/USyncQuery.ts | wassupweb/wausync/query.py | Extended parser parity: now parses both `list` and `side_list` result nodes and adds Baileys-style camelCase method aliases (`withMode`, `withContext`, `withUser`, `parseUSyncQueryResult`, etc.) |
| ported | Baileys/src/WAUSync/USyncUser.ts | wassupweb/wausync/user.py | Added Baileys-style camelCase builder aliases (`withId`, `withLid`, `withPhone`, `withType`, `withPersonaId`) on top of snake_case methods |
| ported | Baileys/WAProto/fix-imports.js | tools/waproto/fix_imports.py | Python post-process helper implemented for generated pb2 import normalization |
| ported | Baileys/WAProto/GenerateStatics.sh | tools/waproto/GenerateStatics.sh | Shell wrapper now drives full Python statics generation pipeline |
| ported | Baileys/WAProto/index.d.ts | wassupweb/waproto/__init__.pyi | Typed proto loader interface completed for generated module namespace |
| ported | Baileys/WAProto/index.js | wassupweb/waproto/__init__.py | Dynamic proto namespace loader completed with runtime `get/has/require` helpers |
| ported | Baileys/WAProto/WAProto.proto | wassupweb/waproto/waproto.proto | Proto schema copied and Python-compatible enum normalization handled during statics generation |

## API/Architecture Tasks

- [x] Define ABC interfaces for transport, auth store, signal store, event bus, media pipeline, and socket layers.
- [x] Build module-level API in wassupweb/__init__.py matching Baileys entrypoints (makeWASocket, defaults, utils, types).
- [~] Port defaults, types, wabinary first to unblock higher-level socket layers.
- [~] Port utils second with cryptography, retries, event buffering, and message transforms.
- [~] Port signal, then socket layers in dependency order.
- [~] Port wam and wausync after socket core compiles.
- [~] Port tests to pytest and establish parity smoke/e2e suite.
